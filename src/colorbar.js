var vertical = 1,
    horizontal = 2;

function colorbar(orient, width, height) {
    
}

export  function colorbarV(width,height) {
    return colorbar(vertical, width, height);
}

export  function colorbarH(width,height) {
    return colorbar(horizontal, width, height);
}
